// TODO : écrire la fonction min()
function min(x, y) {
    var minimum = Math.min(x, y); // Affiche 4.5
    return minimum;
}

console.log(min(4.5, 5)); // Doit afficher 4.5
console.log(min(19, 9)); // Doit afficher 9
console.log(min(1, 1)); // Doit afficher 1